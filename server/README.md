# Mentored Sp21 TGIF Express Server

## Available Scripts
In the project directory, you can run:
### `yarn server`
Starts the express server in your terminal in development mode, automatically reloading upon changes to files using `nodemon`